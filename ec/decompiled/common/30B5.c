// bank0 @ 30B5   or_table_bytes_into_dptr_under_flag_87   [named]
// Ghidra 12.1.3 decompile, generated by ec/tools/build_ec_decompile.py -- do not edit.
// Source: ec/firmware/GMxMGxx_11.800, CODE bank 0 at file 0x08000 + the 0x0000-0x7FFF common area, SHA-256 158d1c6416426939a814146b766a44e2ff0e9286b0abd237e70e51a0c03399c4
// Machine output carrying this repository's symbols. Not the vendor's source.


/* Forty-two bytes, and the one row in the tranche that names no XDATA address of its own: the
   single `movx` at 0x30D8 runs on a DPTR that 0x3B4E set, inside a push and pop of DPH and DPL. It
   runs three times, counting in R7 against upper internal RAM 0x88, 0x89 and 0x8A read through R0;
   on each pass it tests direct bit address 0xE7 -- which is above the bit-addressable internal
   range, so no internal byte behind it is established here: `bit_name()` reads it as `acc.7` and
   the .c folds the test onto the byte at 0x88+R7 that the listing has just read into A -- and on
   every pass but the last calls 0x3B22, 0x3B32 and 0x3B70, pushes DPH and DPL, calls 0x3B4E, reads
   one byte from the code table that call addresses into R5, ORs it with R6, restores DPTR and
   writes the result. The `cjne` at 0x30DB is a three-count loop, and OR-ing R6 into the byte means
   the value already in the caller's XDATA is accumulated into rather than replaced. Whether the R7
   counter is a channel index is not established by this listing, and 0x3B22, 0x3B32, 0x3B4E, 0x3B70
   and the XDATA address are all outside it.
   type: writer
   evidence: ec/decompiled/common/30B5.asm; ec/decompiled/common/30B5.c
   basis: hand-decoded
   name_basis: code-shape */

void or_table_bytes_into_dptr_under_flag_87(byte *param_1,byte param_2)

{
  char cVar1;
  byte bVar2;
  byte *pbVar3;
  
  cVar1 = '\0';
  do {
    if (*(char *)(cVar1 + -0x78) < '\0') {
      mul13_table_lookup_3b22(cVar1);
      load_dptr_high_from_a_low_from_code_table_3b32();
      dph_from_a_dpl_from_code_3b70();
      pbVar3 = param_1;
      bVar2 = read_dptr_to_r6_then_dptr_3a00_plus_13x();
      *param_1 = param_2 | pbVar3[bVar2];
    }
    cVar1 = cVar1 + '\x01';
  } while (cVar1 != '\x03');
  return;
}


