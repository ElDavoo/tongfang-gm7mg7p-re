// bank0 @ 07F0   inc_xdata_0043_return_new   [named]
// Ghidra 12.1.3 decompile, generated by ec/tools/build_ec_decompile.py -- do not edit.
// Source: ec/firmware/GMxMGxx_11.800, CODE bank 0 at file 0x08000 + the 0x0000-0x7FFF common area, SHA-256 158d1c6416426939a814146b766a44e2ff0e9286b0abd237e70e51a0c03399c4
// Machine output carrying this repository's symbols. Not the vendor's source.


/* Six instructions, eight bytes: loads DPTR with 0x0043, reads the byte, increments A, writes it
   back, reads it again and returns, so what comes back is the stored value rather than the one
   before it -- a counter step. 0x0043 and 0x200B have no entry in ec/annotations/registers.yaml; by
   the committed scans both are EC-side rather than PD-side, 0x0043 at four direct `MOV DPTR` sites
   (0x0147, 0x01A4, 0x02B3 and this one -- three bare writes and this read-modify-write) and 0x200B
   at eighteen, so the byte stepped here is written from three other places too. The three comments
   that cite this row -- `common,0x012F`, `common,0x018C` and `common,0x029B` -- each read the
   return as a wait for the value 5. The bytes agree and add one detail those comments do not carry:
   each of the three writes 0 to 0x0043 once before that loop and 0 to 0x200B on every pass of it,
   rather than both on every pass. What the counter counts is not decoded here. Boundary checked
   rather than assumed: the listing's own `ret` at 0x07F7 and the next index row, `FUN_CODE_07f8`,
   agree, so this is a hard end and not the hypothesis an un-annotated listing carries. Nothing was
   read back on hardware: the counter and the returned value are static readings of the
   instructions.
   type: writer
   evidence: ec/decompiled/common/07F0.asm; ec/decompiled/common/07F0.c
   basis: hand-decoded
   name_basis: code-shape */

char inc_xdata_0043_return_new(void)

{
  DAT_EXTMEM_0043 = DAT_EXTMEM_0043 + '\x01';
  return DAT_EXTMEM_0043;
}


