// bank0 @ 0F75   clr_xdata_0000_00ff_iram_20_bf_xdata_9000_97ff   [named]
// Ghidra 12.1.3 decompile, generated by ec/tools/build_ec_decompile.py -- do not edit.
// Source: ec/firmware/GMxMGxx_11.800, CODE bank 0 at file 0x08000 + the 0x0000-0x7FFF common area, SHA-256 158d1c6416426939a814146b766a44e2ff0e9286b0abd237e70e51a0c03399c4
// Machine output carrying this repository's symbols. Not the vendor's source.


/* Thirty-three instructions, 49 bytes: three clearing loops and a `ret`. All three bound on the
   pointer's **high** byte rather than its low one, and that is the whole of the difference from the
   obvious reading. The first sets R7 to 0x20, loads DPTR with 0x0000 and stores a cleared A while
   DPH is below 1, so it clears XDATA 0x0000-0x00FF -- 256 bytes, and not the 32 that R7's immediate
   would suggest; R7 is the second loop's counter, not this one's bound. The second, which the issue
   this row answers does not mention at all, compares R7 against 0xC0 and stores through `mov @R0,A`
   after the `xch A,R0` pair that copies R7 into R0, so it clears **internal** RAM 0x20-0xBF -- 160
   bytes -- and leaves R7 at 0xC0. The third loads DPTR with 0x9000 and compares DPH against 0x98,
   so it clears XDATA 0x9000-0x97FF -- 2048 bytes, and not the 152 a low-byte compare on that
   immediate would give. 0F75.c agrees on all three: its first loop runs while the pointer's high
   byte is zero and its second walks `DAT_INTMEM_20` to 0xC0. Its trailing `return bVar2 + 0x68;` is
   an artifact of that last compare rather than a returned value -- there is nothing in A to read at
   the `ret`. **What clearing these three regions is for is not decoded.** The single inbound site
   is the `lcall 0x0F75` at ec/decompiled/common/0070.asm:13, inside the row named
   `reset_entry_stack_bank0_then_code_init`, which places it between the two bank selects in Keil
   startup and supports no more than that. Boundary checked rather than assumed: the listing's own
   `ret` at 0x0FA5 and the next index row, `FUN_CODE_0fa6`, agree, so this is a hard end and not the
   hypothesis an un-annotated listing carries. A static reading of the instructions; no register was
   observed and nothing was run on hardware.
   type: init
   evidence: ec/decompiled/common/0F75.asm; ec/decompiled/common/0F75.c
   basis: hand-decoded
   name_basis: code-shape */

char clr_xdata_0000_00ff_iram_20_bf_xdata_9000_97ff(void)

{
  undefined1 *puVar1;
  byte bVar2;
  undefined1 *puVar3;
  
  puVar1 = &DAT_INTMEM_20;
  for (puVar3 = (undefined1 *)0x0; (char)((ushort)puVar3 >> 8) == '\0'; puVar3 = puVar3 + 1) {
    *puVar3 = 0;
  }
  for (; puVar1 < (undefined1 *)0xc0; puVar1 = puVar1 + '\x01') {
    *puVar1 = 0;
  }
  for (puVar3 = &DAT_EXTMEM_9000; bVar2 = (byte)((ushort)puVar3 >> 8), bVar2 < 0x98;
      puVar3 = puVar3 + 1) {
    *puVar3 = 0;
  }
  return bVar2 + 0x68;
}


